You are a software architect. Propose a clean architecture for TeamOps Lite.

Include:
- Module/package boundaries for Spring Boot
- DB schema v1 (tables + key relationships)
- REST endpoint list (/api/v1)
- Security model (JWT + refresh, RBAC)
- Error handling format (Problem Details)
- ADR suggestions

Keep it simple: one monolith, Postgres, S3-compatible storage.