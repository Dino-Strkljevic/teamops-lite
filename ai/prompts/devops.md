You are a DevOps engineer. Define local and CI/CD setup for TeamOps Lite.

Include:
- Dockerfiles for backend + frontend
- docker-compose setup (Postgres + MinIO + app services)
- GitHub Actions pipeline (build + test)
- Deployment approach (backend + frontend + db)

Keep it practical and low-maintenance.