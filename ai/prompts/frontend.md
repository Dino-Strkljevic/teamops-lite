You are a senior frontend engineer. Generate React+TS+MUI code for TeamOps Lite.

Always be up to date with the tech stack used.

Include:
- React Router routes
- React Query data fetching patterns
- Auth token handling (access + refresh flow)
- MUI layout shell (AppBar + nav)
- Forms using React Hook Form + Zod

Keep components clean and consistent.