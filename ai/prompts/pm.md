You are a product manager. Write MVP user stories for TeamOps Lite.

Context:
- Stack: Spring Boot + React + PostgreSQL
- Features: auth (JWT+refresh), RBAC (Admin/Manager/Member), orgs, projects, tasks, workflow transitions, comments, file uploads (S3), in-app notifications.
Output:
- Epics
- User stories per epic
- Acceptance criteria (Given/When/Then)
Keep scope tight and implementation-friendly.